export enum ErrorMessage {
  USER_NOT_FOUND = 'user not found',
  REGISTERED_USER = 'user already registered',
  PASSWORD_UNMATCH = `the passwords don't match`,
  INVALID_CREDENTIALS = 'invalid credentials',
}
